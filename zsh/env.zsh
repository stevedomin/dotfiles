export PATH=$PATH:/usr/local/share/npm/bin

# NODE PATH
export NODE_PATH=/usr/local/lib/node

# GOPATH
export GOPATH=/Users/steve/src/go

# for bundled binaries
PATH=.bundle/binstubs:$PATH

PATH=node_modules/.bin:$PATH
