function directory_in_path --description 'Find directory in path'
  set -l directory $argv[1]
  set -l dir $PWD

  while test $dir != '/'
    if test -d $dir/$directory
      echo -n $dir/$directory
      return 0
    end
    set dir (dirname $dir)
  end

  return 1
end
